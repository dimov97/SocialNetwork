import React from "react";

const Header = () => {
    return (
        <div>
            <a href="#">HOME</a>
            <a href="#">NEWS</a>
            <a href="#">MESSAGES</a>

        </div>
    );
}

export default Header;